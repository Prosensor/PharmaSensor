# Copier ce fichier en .env.local pour le développement local
# cp .env.example .env.local

# === OBLIGATOIRE (formulaires contact, devis, tarifs) ===
# Mot de passe SMTP du compte contact@greensensor.fr (hébergeur LWS)
MAIL_PASSWORD=

# === OPTIONNEL (route legacy /api/demo via Resend) ===
RESEND_API_KEY=
CONTACT_EMAIL=info@prosensor.com
